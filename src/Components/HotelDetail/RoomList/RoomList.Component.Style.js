import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    roomListWrapper: {
        flexDirection: "row", 
        alignItems: 'center', 
        justifyContent: 'space-between',
        marginTop: 10
    }
});