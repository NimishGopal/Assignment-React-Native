import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    indicatorWrapper: {
        flex: 1,
        alignItems: "center",
        justifyContent: "center"
    },
    hotelListWrapper: {
        flex: 0.9,
        zIndex: 0,
        // marginTop: 50
    }
});