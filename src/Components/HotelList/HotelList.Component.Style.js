import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    indicatorWrapper: {
        flex: 1,
        alignItems: "center",
        justifyContent: "center"
    },
    hotelListWrapper: {
        flex: 1,
        zIndex: 0
    }
});